set title "Ordenacion Iterativa"

set xlabel "Longitud"
set ylabel "tiempo (s)"

plot "tiemposOrdenacion3.dat" with linespoints title "empirico"

f(x) = a*x + b

fit f(x) "tiemposOrdenacion2.dat" via a,b

replot f(x) title sprintf("ajuste: %.3gx + %.3g", a, b)
